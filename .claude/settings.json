{
  "hooks": {
    "postToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "f=$(echo \"$TOOL_INPUT\" | jq -r '.file_path // empty'); if [[ \"$f\" == *.py ]]; then cd \"$(git rev-parse --show-toplevel)/backend\" && uv run ruff format -- \"$f\" && uv run ruff check --fix -- \"$f\"; fi"
      }
    ]
  }
}
