import styled from "@emotion/styled";
import { Tabs } from "@mui/material";

export const StyledTabs = styled(Tabs)`
  flex: 1;
  padding: 0 24px;

  .MuiTabs-scroller {
    padding: 0;
  }

  .MuiTab-root {
    line-height: 32px;
    margin: 0;
  }
`;
