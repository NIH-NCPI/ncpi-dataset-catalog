services:
  search-api:
    build:
      context: ..
      dockerfile: backend/Dockerfile
      args:
        GIT_SHA: ${GIT_SHA:-unknown}
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000}
      - NCPI_DUCKDB_CACHE_PATH=/cache/concept-search.duckdb
      - NCPI_REPO_ROOT=/data
    volumes:
      - search-cache:/cache
      - ../catalog:/data/catalog:ro
      - ../catalog-build/classification/output/demographic-profiles.json:/data/catalog-build/classification/output/demographic-profiles.json:ro
      - ../catalog-build/classification/output/concept-isa.json:/data/catalog-build/classification/output/concept-isa.json:ro
      - ../catalog-build/classification/output/llm-concepts-v4:/data/catalog-build/classification/output/llm-concepts-v4:ro

volumes:
  search-cache:
